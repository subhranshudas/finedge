// Unit test setup
// No database or external dependencies here — unit tests mock everything inline.
//
// Add here:
// - global jest.mock() calls shared across all unit tests
// - global stubs for third-party modules
// - any test environment globals needed before unit tests run
